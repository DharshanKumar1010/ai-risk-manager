/**
 * Application shell.
 *
 * Deliberately empty. Phase 8 designs this as a risk-operations console — starting
 * from a design-token brainstorm rather than a default template — so Phase 0 commits
 * to no colour, type or layout decisions that Phase 8 would have to undo.
 */
export default function App() {
  return <main aria-label="RiskIQ" />
}
