# RiskIQ — Phase 2 Tier-1 Evaluation

Generated by `python -m app.models.train_tier1`. Every number is measured on the held-out **test** split, which was scored exactly once, after the operating threshold had been chosen on validation.

Random seed: `42`. Latency budget: p95 < 50ms.

---

## ieee_cis

### Tier-1 input definition

- **source_dataset**: `ieee_cis`
- **tier1_feature_version**: `fv_c1d8eb96f693`
- **features**: 113 (82 numeric, 4 frequency-encoded, 27 native categorical)
- **numeric-only matrix** (Isolation Forest / matched LightGBM): 86 features
- **dropped on train measurement** (1): `velocity_available (constant on train)`

Rows: train 413,378, val 88,581, test 88,581.

### Candidate comparison

**Selection reads the `val PR-AUC` column, never the test column.** Test is scored once, after the winner is already chosen, for reporting only — choosing on it would make it a validation set and invalidate the headline.

| candidate | val PR-AUC | test PR-AUC | 95% CI | lift | precision | recall | F1 |
|---|---|---|---|---|---|---|---|
| _no-skill floor (random ranker)_ | — | 0.0348 | — | 1.0x | — | — | — |
| amount-only ranking (reference) | — | 0.0371 | 0.0356–0.0391 | 1.1x | 0.0338 | 0.5021 | 0.0633 |
| IsolationForest (unsupervised) | 0.1447 | 0.0886 | 0.0847–0.0930 | 2.5x | 0.0435 | 0.8534 | 0.0828 |
| LightGBM (matched inputs) | 0.5940 | 0.5119 | 0.4955–0.5303 | 14.7x | 0.1049 | 0.8780 | 0.1875 |
| LightGBM (full inputs) **(selected)** | 0.6155 | 0.5276 | 0.5117–0.5462 | 15.2x | 0.1055 | 0.8771 | 0.1884 |

**Supervision** is worth a PR-AUC delta of 95% CI [0.4241, 0.4558] over the unsupervised baseline. The interval excludes zero, so the difference is real at this sample size.

**The selected model over the runner-up** (LightGBM (matched inputs)): delta 95% CI [0.0105, 0.0211]. The interval excludes zero, so the selection is justified.

### What the selected model leans on

Share of total split gain. A model resting almost entirely on one or two features is the signature of a dataset artefact rather than a learned pattern.

| feature | share of gain |
|---|---|
| `C1` | 7.2% |
| `C14` | 4.6% |
| `C13` | 4.3% |
| `card1` | 4.3% |
| `card2` | 4.2% |
| `addr1` | 3.8% |
| `D2` | 3.5% |
| `C4` | 2.9% |
| `amount_log` | 2.4% |
| `velocity_sum_7d` | 2.4% |
| `D15` | 2.4% |
| `amount_prior_mean` | 2.3% |

### Full results

```
### amount-only ranking (reference) — held-out test (n=88,581, positives=3,083, base rate=3.4804%)
Threshold: 0.458065  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.0371  (95% CI 0.0356-0.0391)
           no-skill floor 0.0348 (1.1x lift)
Precision  0.0338      Recall  0.5021      F1  0.0633

Confusion matrix        Predicted
                     neg        pos
Actual  neg     41,252     44,246
        pos      1,535      1,548

False-positive cost estimate: 2,332.77 per 1,000 transactions  [IEEE-CIS amount units (consistent with USD)]
  44,246 false positives x 3.00 = 132,738.00
  1,535 false negatives (amount + 15.00) = 73,900.75
  total = 206,638.75 over 88,581 transactions
  flag rate = 51.70% (45,794 sent for review)
  Assumptions:
    - A false positive costs 3.00 IEEE-CIS amount units (consistent with USD) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 15.00 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Not a model. The score is `amount_log`, rank-scaled.
```

```
### IsolationForest (unsupervised) — held-out test (n=88,581, positives=3,083, base rate=3.4804%)
Threshold: 0.078534  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.0886  (95% CI 0.0847-0.0930)
           no-skill floor 0.0348 (2.5x lift)
Precision  0.0435      Recall  0.8534      F1  0.0828

Confusion matrix        Predicted
                     neg        pos
Actual  neg     27,633     57,865
        pos        452      2,631

False-positive cost estimate: 2,672.92 per 1,000 transactions  [IEEE-CIS amount units (consistent with USD)]
  57,865 false positives x 3.00 = 173,595.00
  452 false negatives (amount + 15.00) = 63,174.91
  total = 236,769.91 over 88,581 transactions
  flag rate = 68.29% (60,496 sent for review)
  Assumptions:
    - A false positive costs 3.00 IEEE-CIS amount units (consistent with USD) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 15.00 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Sees no labels. Kept as the cold-start reference: chargeback labels arrive weeks after the transaction, so this is what is achievable on a merchant with no labelled history yet.
```

```
### LightGBM (matched inputs) — held-out test (n=88,581, positives=3,083, base rate=3.4804%)
Threshold: 0.009495  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.5119  (95% CI 0.4955-0.5303)
           no-skill floor 0.0348 (14.7x lift)
Precision  0.1049      Recall  0.8780      F1  0.1875

Confusion matrix        Predicted
                     neg        pos
Actual  neg     62,407     23,091
        pos        376      2,707

False-positive cost estimate: 1,418.32 per 1,000 transactions  [IEEE-CIS amount units (consistent with USD)]
  23,091 false positives x 3.00 = 69,273.00
  376 false negatives (amount + 15.00) = 56,363.19
  total = 125,636.19 over 88,581 transactions
  flag rate = 29.12% (25,798 sent for review)
  Assumptions:
    - A false positive costs 3.00 IEEE-CIS amount units (consistent with USD) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 15.00 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Fitted on the same numeric-only matrix as the Isolation Forest, so the gap between the two is supervision alone rather than a difference in inputs.
```

```
### LightGBM (full inputs) — held-out test (n=88,581, positives=3,083, base rate=3.4804%)
Threshold: 0.004716  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.5276  (95% CI 0.5117-0.5462)
           no-skill floor 0.0348 (15.2x lift)
Precision  0.1055      Recall  0.8771      F1  0.1884

Confusion matrix        Predicted
                     neg        pos
Actual  neg     62,574     22,924
        pos        379      2,704

False-positive cost estimate: 1,362.06 per 1,000 transactions  [IEEE-CIS amount units (consistent with USD)]
  22,924 false positives x 3.00 = 68,772.00
  379 false negatives (amount + 15.00) = 51,880.57
  total = 120,652.57 over 88,581 transactions
  flag rate = 28.93% (25,628 sent for review)
  Assumptions:
    - A false positive costs 3.00 IEEE-CIS amount units (consistent with USD) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 15.00 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.
```

### Selected model at a staffable operating point

The threshold above minimises estimated cost, which — with review capacity assumed unbounded — can imply a queue no team could work. The same model, capped at 1.0% of traffic:

```
### LightGBM (full inputs) @ capacity — held-out test (n=88,581, positives=3,083, base rate=3.4804%)
Threshold: 0.813710  (chosen on validation by flagging at most 1.0% of validation traffic)

PR-AUC     0.5276
           no-skill floor 0.0348 (15.2x lift)
Precision  0.8734      Recall  0.2462      F1  0.3841

Confusion matrix        Predicted
                     neg        pos
Actual  neg     85,388        110
        pos      2,324        759

False-positive cost estimate: 4,922.50 per 1,000 transactions  [IEEE-CIS amount units (consistent with USD)]
  110 false positives x 3.00 = 330.00
  2,324 false negatives (amount + 15.00) = 435,709.85
  total = 436,039.85 over 88,581 transactions
  flag rate = 0.98% (869 sent for review)
  Assumptions:
    - A false positive costs 3.00 IEEE-CIS amount units (consistent with USD) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 15.00 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

The same model at a staffable operating point. Reported because the cost-minimising threshold above assumes unbounded review capacity.
```

### Cost sensitivity — selected model, thresholds re-chosen on validation

```
Cost sensitivity, +/-50% on BOTH parameters (threshold re-chosen on validation):
  factor   threshold        total cost        FP        FN   flag rate
    0.5x      0.001584         73,627.62    38,826       129     47.12%
    1.0x      0.004716        108,329.17    20,598       295     26.35%
    1.5x      0.005968        140,995.35    17,408       353     22.69%

Cost sensitivity, review cost ONLY (the direction that moves the recommendation):
  factor   threshold        total cost        FP        FN   flag rate
    1.0x      0.004716        108,329.17    20,598       295     26.35%
    5.0x      0.035352        218,389.02     3,554       918      6.41%
   25.0x      0.113935        323,404.67     1,022     1,318      3.10%
  100.0x      0.457114        403,862.56       202     1,861      1.56%
```

### Latency — selected model

```
100 sequential score() calls
  p50  5.620 ms
  p95  6.384 ms   (budget 50 ms)
  p99  6.856 ms
  max  8.276 ms
```

Measures the scoring call only — vector in, decision out. Feature assembly is Phase 7's scoring endpoint and is budgeted separately.

---

## paysim

### Tier-1 input definition

- **source_dataset**: `paysim`
- **tier1_feature_version**: `fv_7bb1251837e5`
- **features**: 14 (13 numeric, 0 frequency-encoded, 1 native categorical)
- **numeric-only matrix** (Isolation Forest / matched LightGBM): 13 features
- **post-settlement features** (present in corpus, absent at ingestion): `newbalanceOrig`, `newbalanceDest`, `balance_error_orig`, `balance_error_dest`, `drains_origin_balance`
- **dropped on train measurement** (11): `seconds_since_prior_txn (99.95% null on train)`, `amount_prior_mean (99.95% null on train)`, `amount_zscore_vs_own_history (100.00% null on train)`, `velocity_count_1h (100.00% null on train)`, `velocity_sum_1h (100.00% null on train)`, `velocity_count_24h (100.00% null on train)`, `velocity_sum_24h (100.00% null on train)`, `velocity_count_7d (100.00% null on train)`, `velocity_sum_7d (100.00% null on train)`, `velocity_available (constant on train)`, `dest_is_merchant (constant on train)`

Rows: train 1,938,484, val 415,628, test 416,297.

### Candidate comparison

**Selection reads the `val PR-AUC` column, never the test column.** Test is scored once, after the winner is already chosen, for reporting only — choosing on it would make it a validation set and invalidate the headline.

| candidate | val PR-AUC | test PR-AUC | 95% CI | lift | precision | recall | F1 |
|---|---|---|---|---|---|---|---|
| _no-skill floor (random ranker)_ | — | 0.0097 | — | 1.0x | — | — | — |
| amount-only ranking (reference) | — | 0.1213 | 0.1111–0.1323 | 12.6x | 0.0583 | 0.3963 | 0.1016 |
| IsolationForest (unsupervised) | 0.0070 | 0.0794 | 0.0743–0.0862 | 8.2x | 0.0735 | 0.4002 | 0.1242 |
| LightGBM (matched inputs) **(selected)** | 1.0000 | 0.9998 | 0.9997–0.9999 | 103.5x | 0.9705 | 0.9983 | 0.9842 |
| LightGBM (full inputs) | 1.0000 | 0.9999 | 0.9998–1.0000 | 103.5x | 0.9997 | 0.9938 | 0.9968 |
| LightGBM (ingestion-safe) | 0.8480 | 0.9386 | 0.9334–0.9435 | 97.2x | 0.6952 | 0.9276 | 0.7948 |

**Supervision** is worth a PR-AUC delta of 95% CI [0.9136, 0.9255] over the unsupervised baseline. The interval excludes zero, so the difference is real at this sample size.

**The selected model over the runner-up** (LightGBM (full inputs)): delta 95% CI [-0.0002, -0.0000]. **The interval includes zero: on this test split the runner-up performs as well, and the selection rests on a point estimate inside the noise. The simpler candidate is the defensible choice where the two differ in cost or complexity.**

### What the selected model leans on

Share of total split gain. A model resting almost entirely on one or two features is the signature of a dataset artefact rather than a learned pattern.

| feature | share of gain |
|---|---|
| `freq_transaction_type` | 40.1% |
| `hour_of_day` | 21.7% |
| `oldbalanceDest` | 17.0% |
| `newbalanceDest` | 11.4% |
| `amount_log` | 2.8% |
| `drains_origin_balance` | 2.4% |
| `oldbalanceOrg` | 1.9% |
| `newbalanceOrig` | 1.5% |
| `balance_error_orig` | 1.0% |
| `day_of_week` | 0.3% |
| `balance_error_dest` | 0.0% |
| `counterparty_prior_txn_count` | 0.0% |

### Full results

```
### amount-only ranking (reference) — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.819066  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.1213  (95% CI 0.1111-0.1323)
           no-skill floor 0.0097 (12.6x lift)
Precision  0.0583      Recall  0.3963      F1  0.1016

Confusion matrix        Predicted
                     neg        pos
Actual  neg    386,539     25,738
        pos      2,427      1,593

False-positive cost estimate: 2,083,294.91 per 1,000 transactions  [PaySim simulator units (not currency)]
  25,738 false positives x 7,454.73 = 191,869,940.91
  2,427 false negatives (amount + 37,273.67) = 675,399,480.64
  total = 867,269,421.54 over 416,297 transactions
  flag rate = 6.57% (27,331 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Not a model. The score is `amount_log`, rank-scaled.
```

```
### IsolationForest (unsupervised) — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.481331  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.0794  (95% CI 0.0743-0.0862)
           no-skill floor 0.0097 (8.2x lift)
Precision  0.0735      Recall  0.4002      F1  0.1242

Confusion matrix        Predicted
                     neg        pos
Actual  neg    391,998     20,279
        pos      2,411      1,609

False-positive cost estimate: 3,014,245.21 per 1,000 transactions  [PaySim simulator units (not currency)]
  20,279 false positives x 7,454.73 = 151,174,548.59
  2,411 false negatives (amount + 37,273.67) = 1,103,646,689.70
  total = 1,254,821,238.30 over 416,297 transactions
  flag rate = 5.26% (21,888 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Sees no labels. Kept as the cold-start reference: chargeback labels arrive weeks after the transaction, so this is what is achievable on a merchant with no labelled history yet.
```

```
### LightGBM (matched inputs) — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.356025  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.9998  (95% CI 0.9997-0.9999)
           no-skill floor 0.0097 (103.5x lift)
Precision  0.9705      Recall  0.9983      F1  0.9842

Confusion matrix        Predicted
                     neg        pos
Actual  neg    412,155        122
        pos          7      4,013

False-positive cost estimate: 11,499.90 per 1,000 transactions  [PaySim simulator units (not currency)]
  122 false positives x 7,454.73 = 909,477.53
  7 false negatives (amount + 37,273.67) = 3,877,897.51
  total = 4,787,375.04 over 416,297 transactions
  flag rate = 0.99% (4,135 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

!! PR-AUC exceeds 0.95 on fraud data. Treat as a suspected leak until disproven (ml-evaluation-standards section 4).

Fitted on the same numeric-only matrix as the Isolation Forest, so the gap between the two is supervision alone rather than a difference in inputs.
```

```
### LightGBM (full inputs) — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.403122  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.9999  (95% CI 0.9998-1.0000)
           no-skill floor 0.0097 (103.5x lift)
Precision  0.9997      Recall  0.9938      F1  0.9968

Confusion matrix        Predicted
                     neg        pos
Actual  neg    412,276          1
        pos         25      3,995

False-positive cost estimate: 59,402.99 per 1,000 transactions  [PaySim simulator units (not currency)]
  1 false positives x 7,454.73 = 7,454.73
  25 false negatives (amount + 37,273.67) = 24,721,831.56
  total = 24,729,286.29 over 416,297 transactions
  flag rate = 0.96% (3,996 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

!! PR-AUC exceeds 0.95 on fraud data. Treat as a suspected leak until disproven (ml-evaluation-standards section 4).
```

```
### LightGBM (ingestion-safe) — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.015466  (chosen on validation by minimising estimated cost on validation)

PR-AUC     0.9386  (95% CI 0.9334-0.9435)
           no-skill floor 0.0097 (97.2x lift)
Precision  0.6952      Recall  0.9276      F1  0.7948

Confusion matrix        Predicted
                     neg        pos
Actual  neg    410,642      1,635
        pos        291      3,729

False-positive cost estimate: 141,811.52 per 1,000 transactions  [PaySim simulator units (not currency)]
  1,635 false positives x 7,454.73 = 12,188,489.91
  291 false negatives (amount + 37,273.67) = 46,847,218.42
  total = 59,035,708.34 over 416,297 transactions
  flag rate = 1.29% (5,364 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

Post-settlement balance features removed: `newbalanceOrig`, `newbalanceDest`, `balance_error_orig`, `balance_error_dest`, `drains_origin_balance`. A real ingestion-time scorer does not have the balance a transaction settles to. The gap against the full model is the measured size of that advantage, and this is the deployable number.
```

### Selected model at a staffable operating point

The threshold above minimises estimated cost, which — with review capacity assumed unbounded — can imply a queue no team could work. The same model, capped at 1.0% of traffic:

```
### LightGBM (matched inputs) @ capacity — held-out test (n=416,297, positives=4,020, base rate=0.9657%)
Threshold: 0.000263  (chosen on validation by flagging at most 1.0% of validation traffic)

PR-AUC     0.9998
           no-skill floor 0.0097 (103.5x lift)
Precision  0.2729      Recall  1.0000      F1  0.4288

Confusion matrix        Predicted
                     neg        pos
Actual  neg    401,569     10,708
        pos          0      4,020

False-positive cost estimate: 191,750.82 per 1,000 transactions  [PaySim simulator units (not currency)]
  10,708 false positives x 7,454.73 = 79,825,290.51
  0 false negatives (amount + 37,273.67) = 0.00
  total = 79,825,290.51 over 416,297 transactions
  flag rate = 3.54% (14,728 sent for review)
  Assumptions:
    - A false positive costs 7,454.73 PaySim simulator units (not currency) - analyst time on a manual review, or the lost margin and friction of declining a good customer.
    - A false negative costs the transaction amount plus a flat 37,273.67 - the chargeback fee and internal handling on top of the value lost.
    - Cost is linear in the number of mistakes: no queue-congestion effect, no customer-churn effect from repeated false declines, no recovery on disputed fraud. All three would raise the true cost of a false positive.
    - Every fraud is assumed to charge back. In practice some share is never disputed, which would lower false-negative cost.
    - Review capacity is unbounded. This is the assumption that bites hardest: the cost-minimising threshold below flags whatever share of traffic the arithmetic favours, with no ceiling on the queue it implies.

!! PR-AUC exceeds 0.95 on fraud data. Treat as a suspected leak until disproven (ml-evaluation-standards section 4).

The same model at a staffable operating point. Reported because the cost-minimising threshold above assumes unbounded review capacity.
```

### Cost sensitivity — selected model, thresholds re-chosen on validation

```
Cost sensitivity, +/-50% on BOTH parameters (threshold re-chosen on validation):
  factor   threshold        total cost        FP        FN   flag rate
    0.5x      0.356025              0.00         0         0      0.13%
    1.0x      0.356025              0.00         0         0      0.13%
    1.5x      0.356025              0.00         0         0      0.13%

Cost sensitivity, review cost ONLY (the direction that moves the recommendation):
  factor   threshold        total cost        FP        FN   flag rate
    1.0x      0.356025              0.00         0         0      0.13%
    5.0x      0.356025              0.00         0         0      0.13%
   25.0x      0.356025              0.00         0         0      0.13%
  100.0x      0.356025              0.00         0         0      0.13%
```

### Latency — selected model

```
100 sequential score() calls
  p50  0.170 ms
  p95  0.285 ms   (budget 50 ms)
  p99  0.329 ms
  max  0.613 ms
```

Measures the scoring call only — vector in, decision out. Feature assembly is Phase 7's scoring endpoint and is budgeted separately.
