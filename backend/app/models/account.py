"""ORM model for the ``accounts`` table.

Phase 1 defines the schema: account identity plus the rolling behavioural baselines that
Tier-2 sequences and Tier-3 graph edges are built from.

Row-Level Security is required on this table — see
``.claude/skills/security-checklist/SKILL.md`` section 3.
"""
